import Spinner from "../Spinner/Spinner";

const ViewHeightSpinner = () => {
    return (
        <div className="min-h-[calc(100vh-96px-449px)] flex items-center justify-center">
            <Spinner />
        </div>
    );
};

export default ViewHeightSpinner;
